<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Pantry Information</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>This is <%= user.username %> Pantry</h1>

      <ul>
        <% user.pantry.forEach(item => { %>
          <li><%= item.name %></li>
        <% }) %>
      </ul>

  </body>
</html>
